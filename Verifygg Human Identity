on:
  workflow_dispatch:
    inputs:
      agent_name:
        description: 'finder'
        required: true
        default: 'finder'
      agent_description:
        description: 'finder'
        required: true
        default: 'finder AI Agent'
